{
  "permissions": {
    "allow": [
      "Bash(docker compose --env-file .env.production --profile all build prefect-register)",
      "Bash(docker run --rm prefecthq/prefect:3-python3.11 cat /etc/os-release)"
    ]
  }
}
