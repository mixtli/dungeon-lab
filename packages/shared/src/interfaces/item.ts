/**
 * Re-export Item types from schema
 * Game system plugins can extend these types to add system-specific properties
 */
export { Item, ItemCreateData, ItemUpdateData } from '../schemas/item.schema.js'; 