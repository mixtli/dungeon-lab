/**
 * Re-export User types from schema
 * These types define the structure of users in the system
 */
export {
  User,
  UserPreferences,
  UserCreateData,
  UserUpdateData,
  UserTheme,
} from '../schemas/user.schema.js'; 